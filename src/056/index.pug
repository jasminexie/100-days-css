doctype html
html
  head
    style
      include index.css
  body
    .frame
      .flower(onclick="void(0)")
        - var x = 15
          while x > 0
            div(class=`petal petal-${x}`)
            - x -= 1
          .circle
